# 현재 버그 상황 정리 (외식/배달 관련 UI 이슈)

현재 2가지 주요 버그가 발생하고 있으며, 이전 커밋으로 롤백(리셋)한 상태입니다.
다음 AI 세션이나 외부 AI에게 문제 해결을 맡길 때 아래 내용을 참고하세요.

## 1. 장소 추가 시 "목업 데이터(가짜 데이터)"가 1초간 깜빡이는 현상
**[증상]**
외식(Dine-out) 기록을 위해 음식 사진을 업로드하면, "육개장 역삼점", "명가 육개장 강남본점" 같은 가짜(목업) 데이터가 1초 정도 보였다가 진짜 GPS 주변 장소로 바뀜.

**[원인 분석]**
- 사진을 업로드하면 AI가 메뉴명(예: "육개장")을 분석해서 `add-log-modal.tsx`의 검색어(`placeSearchQuery`)로 자동 세팅합니다.
- 이때 GPS API(`loadGpsNearbyPlaces`)가 진짜 장소 데이터를 가져오기까지 약 1초 정도의 딜레이가 발생합니다.
- `add-log-modal.tsx` 파일의 `filteredPlaces` 계산 로직(약 260번 줄)을 보면, **"검색어가 있는데 검색 결과가 없을 경우 임의로 '역삼점', '강남본점' 등의 하드코딩된 샘플 배열을 반환"**하도록 작성되어 있습니다.
- API 로딩이 완료되기 전까지 이 조건이 충족되면서 하드코딩된 목업 리스트가 화면에 렌더링되고, 1초 뒤 API 응답이 오면 진짜 리스트로 교체되기 때문에 깜빡임이 발생하는 것입니다.

**[해결 방향]**
`add-log-modal.tsx`의 `filteredPlaces` 로직 내에 있는 하드코딩 샘플 데이터 블록을 완전히 제거하거나, `isLoadingLocation`이 true일 때는 샘플 데이터를 그리지 않도록 조건을 수정해야 합니다.

---

## 2. 수정한 먹로그 카드에서 "식당 주소(시/동)"가 절대 나오지 않는 현상
**[증상]**
먹로그 목록(Feed)에서 식당 이름 뒤에 `(신당동)` 과 같이 지역명이 나와야 하는데, 글을 한 번 '수정'하고 나면 지역명이 영구적으로 사라져서 절대 나오지 않음.

**[원인 분석]**
- 먹로그 목록(`meal-log-tab.tsx`)에서 특정 글의 수정 아이콘(연필)을 누르면 `handleEditClick` 함수가 실행됩니다.
- 이 함수에서 수정 모달로 데이터를 넘겨줄 때, 식당 주소를 불러오지 않고 **`address: ""`로 강제 초기화(하드코딩)**해서 넘기고 있습니다. (`meal-log-tab.tsx` 약 1141번 줄)
- 모달 창에는 주소가 빈칸으로 들어가고, 사용자가 그대로 '수정 완료'를 누르면 빈칸 주소가 DB에 그대로 덮어씌워져서 기존의 진짜 주소가 영구적으로 날아갑니다.
- DB에 주소가 없으니 렌더링 시 `formatPlaceNameWithRegion` 함수가 지역명을 파싱하지 못하고 식당 이름만 덩그러니 출력하게 됩니다.

**[해결 방향]**
1. `meal-log-tab.tsx`의 `handleEditClick` 함수에서 `placeAddress`를 누락 없이 온전히 담아서 `editData`로 넘기도록 수정해야 합니다. (예: `address: meal.placeAddress || ""`)
2. 더불어 외식/배달의 경우 식당 이름 바(Bar) 영역이 `meal.linkUrl`이 있을 때만 렌더링되게 되어 있는 조건도, 링크 없이 장소만 추가한 경우를 위해 렌더링되도록 수정해야 합니다. (`meal-log-tab.tsx` 약 1738번 줄)

---
**[현재 조치 상태]**
문제의 원인이 꼬이지 않도록 이전에 시도했던 패치들을 `a56bd749` 커밋으로 깨끗하게 하드 리셋해 두었습니다. 위 분석 내용을 바탕으로 코드를 수정하면 완벽하게 해결될 것입니다.

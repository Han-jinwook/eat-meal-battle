# 🚀 바이브코딩 이니셜 시스템 (Vibe Coding Initial System)

## 📋 개요
- **목적**: 빠른 개발과 명확한 구분을 위한 한 글자 접두사 체계
- **원칙**: 이니셜 하나로 파일/폴더 타입 즉시 구분
- **효과**: 타이핑 속도 향상, 자동완성 최적화, 멘탈 모델 단순화

---

## 🗂️ 폴더/파일 구조

### **📋 이니셜 적용 원칙**
- **단일 목적 폴더**: 이니셜 접두사 적용 (예: p-home, c-button)
- **복합 목적 폴더**: 이니셜 없이 기본 이름 유지 (예: app, src, public)
- **프레임워크 폴더**: Next.js 등 프레임워크 규칙 준수 (예: app, pages, api)

### **🔤 폴더 자동 정렬 효과**
```
📁 Before (혼재)          📁 After (이니셜 그룹화)
📁 battle/               📁 a-auth/
📁 components/           📁 a-battle/  
📁 api/                  📁 a-users/
📁 home/                 📁 c-button/
📁 utils/                📁 c-header/
📁 hooks/                📁 c-modal/
📁 profile/              📁 h-auth/
📁 auth/                 📁 p-home/
                         📁 p-profile/
                         📁 u-date/
```
**→ 알파벳 순 자동 정렬로 카테고리별 그룹화!**

### **P- (Pages) 페이지** (예: p-home)
```
📁 src/app/
  📁 p-home/page.tsx          // 홈페이지
  📁 p-login/page.tsx         // 로그인
  📁 p-battle/page.tsx        // 배틀
  📁 p-profile/page.tsx       // 프로필
  📁 p-settings/page.tsx      // 설정
  📁 p-search/page.tsx        // 검색
```

### **A- (API) API 엔드포인트** (예: a-users)
```
📁 src/app/api/
  📁 a-users/route.ts         // 사용자 API
  📁 a-battle/route.ts        // 배틀 API
  📁 a-meals/route.ts         // 급식 API
  📁 a-schools/route.ts       // 학교 API
  📁 a-auth/route.ts          // 인증 API
```

### **C- (Components) 컴포넌트** (예: c-header)
```
📁 src/components/
  📁 c-header/Header.tsx      // 헤더
  📁 c-modal/Modal.tsx        // 모달
  📁 c-button/Button.tsx      // 버튼
  📁 c-card/Card.tsx          // 카드
  📁 c-form/Form.tsx          // 폼
```

### **U- (Utils) 유틸리티** (예: u-date)
```
📁 src/utils/
  📁 u-date/index.ts          // 날짜 유틸
  📁 u-format/index.ts        // 포맷 유틸
  📁 u-validate/index.ts      // 검증 유틸
  📁 u-api/index.ts           // API 유틸
  📁 u-storage/index.ts       // 스토리지 유틸
```

### **H- (Hooks) 커스텀 훅** (예: h-auth)
```
📁 src/hooks/
  📁 h-auth/useAuth.ts        // 인증 훅
  📁 h-data/useData.ts        // 데이터 훅
  📁 h-form/useForm.ts        // 폼 훅
  📁 h-modal/useModal.ts      // 모달 훅
```

### **M- (Markdown) 문서** (예: m-readme)
```
📁 프로젝트 루트/
  📁 m-readme/README.md       // 프로젝트 설명
  📁 m-api/API.md            // API 문서
  📁 m-deploy/DEPLOY.md      // 배포 가이드
  📁 m-changelog/CHANGELOG.md // 변경 이력
  📁 m-conventions/CODING_CONVENTIONS.md // 코딩 규칙
```

### **🏗️ 이니셜 없는 기본 폴더들** (프레임워크/시스템 폴더)
```
📁 프로젝트 구조/
  📁 src/                     // 소스 코드 루트
  📁 app/                     // Next.js App Router
  📁 public/                  // 정적 파일
  📁 node_modules/            // 패키지 의존성
  📁 .next/                   // Next.js 빌드 파일
  📁 .git/                    // Git 버전 관리
  📁 dist/                    // 빌드 결과물
  📁 build/                   // 빌드 결과물
  📁 coverage/                // 테스트 커버리지
  📁 docs/                    // 문서 (또는 m- 접두사 사용)
```

### **S- (Stores) 상태 관리** (예: s-auth) *대규모 앱용
```
📁 src/stores/
  📁 s-auth/authStore.ts      // 인증 스토어 (Zustand/Redux)
  📁 s-user/userStore.ts      // 사용자 스토어
  📁 s-battle/battleStore.ts  // 배틀 스토어
  📁 s-global/globalStore.ts  // 전역 상태
```

### **E- (Enums) 열거형** (예: e-status) *TypeScript 프로젝트용
```
📁 src/enums/
  📁 e-status/index.ts        // 상태 열거형
  📁 e-roles/index.ts         // 역할 열거형
  📁 e-types/index.ts         // 타입 열거형
  📁 e-errors/index.ts        // 에러 코드 열거형
```

---

## 🗄️ 데이터베이스 테이블

### **T- (Tables) 테이블명** (예: t_users)
```sql
-- 사용자 관련
t_users              -- 사용자
t_user_profiles      -- 사용자 프로필
t_user_settings      -- 사용자 설정

-- 학교 관련
t_schools            -- 학교 정보
t_school_meals       -- 급식 정보
t_interest_schools   -- 관심 학교

-- 배틀 관련
t_battles_daily      -- 일별 배틀
t_battles_monthly    -- 월별 배틀
t_battle_results     -- 배틀 결과

-- 평가 관련
t_ratings            -- 평점
t_rating_stats       -- 평점 통계
```

---

## 💻 코드 내 타입/인터페이스

### **T- (Types) 타입 정의** (예: TUser)
```typescript
// 인터페이스
interface TUser {
  id: string;
  name: string;
  email: string;
}

interface TBattle {
  id: string;
  date: string;
  participants: TUser[];
}

interface TApiResponse<T> {
  data: T;
  success: boolean;
  message?: string;
}

// 타입 별칭
type TUserRole = 'student' | 'teacher' | 'admin';
type TBattleStatus = 'pending' | 'active' | 'completed';
type TApiMethod = 'GET' | 'POST' | 'PUT' | 'DELETE';
```

### **E- (Enums) 열거형** (예: EUserRole)
```typescript
enum EUserRole {
  STUDENT = 'student',
  TEACHER = 'teacher',
  ADMIN = 'admin'
}

enum EBattleType {
  DAILY = 'daily',
  MONTHLY = 'monthly',
  SPECIAL = 'special'
}

enum EApiStatus {
  SUCCESS = 200,
  BAD_REQUEST = 400,
  UNAUTHORIZED = 401,
  NOT_FOUND = 404,
  SERVER_ERROR = 500
}
```

---

## 🎯 사용 예시

### **1. 페이지 생성**
```bash
# 새 페이지 생성
mkdir src/app/p-rankings
touch src/app/p-rankings/page.tsx
```

### **2. API 생성**
```bash
# 새 API 생성
mkdir src/app/api/a-rankings
touch src/app/api/a-rankings/route.ts
```

### **3. 컴포넌트 생성**
```bash
# 새 컴포넌트 생성
mkdir src/components/c-ranking-card
touch src/components/c-ranking-card/RankingCard.tsx
```

### **4. Import 예시**
```typescript
// 명확한 구분으로 가독성 향상
import HomePage from '@/app/p-home/page'
import { getUserAPI } from '@/app/api/a-users/route'
import Button from '@/components/c-button/Button'
import { formatDate } from '@/utils/u-date'
import { useAuth } from '@/hooks/h-auth/useAuth'
import { authStore } from '@/stores/s-auth/authStore'
```

---

## 🚀 IDE 최적화 팁

### **1. 폴더 자동 정렬의 실제 효과**
```bash
# VSCode 파일 탐색기에서 자동 그룹화
📁 a-auth/        ← API 관련 찾을 때 여기만 보면 됨
📁 a-battle/
📁 a-users/
---
📁 c-button/      ← 컴포넌트 찾을 때 여기만 보면 됨  
📁 c-header/
📁 c-modal/
---
📁 p-home/        ← 페이지 찾을 때 여기만 보면 됨
📁 p-login/
📁 p-profile/
```

### **2. 자동완성 최적화**
```typescript
// Ctrl+P 누르고 입력하면 즉시 필터링
"a-"  → a-auth/, a-battle/, a-users/     (API만)
"c-"  → c-button/, c-header/, c-modal/   (컴포넌트만)  
"p-"  → p-home/, p-login/, p-profile/    (페이지만)
"u-"  → u-date/, u-format/, u-validate/  (유틸만)
```

### **3. VSCode 스니펫**
```json
{
  "Create Page": {
    "prefix": "p-",
    "body": [
      "export default function ${1:PageName}() {",
      "  return (",
      "    <div>",
      "      <h1>${1:PageName}</h1>",
      "    </div>",
      "  )",
      "}"
    ]
  },
  "Create API": {
    "prefix": "a-",
    "body": [
      "import { NextRequest, NextResponse } from 'next/server'",
      "",
      "export async function GET(request: NextRequest) {",
      "  return NextResponse.json({ message: 'Hello from ${1:ApiName}' })",
      "}"
    ]
  }
}
```

### **4. 검색 패턴**
```bash
# 특정 타입 파일만 검색
find . -name "p-*"     # 모든 페이지
find . -name "a-*"     # 모든 API
find . -name "c-*"     # 모든 컴포넌트
```

---

## 📝 네이밍 규칙

### **파일명**
- **폴더**: `kebab-case` (소문자 + 하이픈)
- **파일**: `PascalCase` for components, `camelCase` for utils
- **접두사**: 항상 소문자 + 하이픈

### **변수명**
- **컴포넌트**: `PascalCase`
- **함수/변수**: `camelCase`
- **상수**: `UPPER_SNAKE_CASE`
- **타입/인터페이스**: `T` + `PascalCase`

### **예시**
```typescript
// ✅ 올바른 예시
const p-home/HomePage.tsx
const a-users/route.ts
const c-button/Button.tsx
const u-date/formatDate.ts

interface TUser { }
type TApiResponse = { }
enum EUserRole { }

// ❌ 잘못된 예시
const home/page.tsx
const users-api/route.ts
const Button/button.tsx
const dateUtils/format.ts
```

---

## 🎉 장점 요약

### **⚡ 개발 속도 향상**
1. **3글자로 카테고리 구분**: `p-` + Tab으로 즉시 페이지 찾기
2. **IDE 자동완성 최적화**: 타입별 필터링으로 빠른 탐색
3. **폴더 자동 정렬**: 알파벳 순 그룹화로 시각적 정리

### **🧠 멘탈 모델 단순화**  
4. **한눈에 파일 타입 구분**: 이니셜만 보고 즉시 파악
5. **일관된 패턴**: 모든 프로젝트에서 동일한 규칙 적용
6. **직관적 구조**: 새 팀원도 쉽게 이해 가능

### **👥 협업 효율성**
7. **명확한 소통**: "p-home 수정해주세요" → 즉시 이해
8. **코드 리뷰 용이**: 파일 위치 예측 가능
9. **온보딩 단축**: 새 개발자 적응 시간 단축

### **🔍 실제 사용 시나리오**
```bash
# 상황: 로그인 페이지 수정 요청
1. Ctrl+P → "p-log" 입력 → p-login/ 즉시 찾기 ⚡
2. 로그인 API 확인 필요 → "a-auth" 입력 → a-auth/ 즉시 찾기 ⚡  
3. 로그인 버튼 컴포넌트 수정 → "c-but" 입력 → c-button/ 즉시 찾기 ⚡

# 기존 방식이라면?
1. 폴더 하나씩 뒤져가며 login 관련 파일 찾기 😵
2. components 폴더에서 Button 관련 파일 찾기 😵
3. api 폴더에서 auth 관련 파일 찾기 😵
```

---

## 🚨 주의사항

1. **일관성 유지**: 한 번 정한 규칙은 프로젝트 전체에 적용
2. **팀 합의**: 팀원 모두가 동의한 후 적용
3. **문서화**: README에 규칙 명시
4. **점진적 적용**: 기존 프로젝트는 새 파일부터 적용

---

**🎯 다음 프로젝트부터 바로 적용 가능한 완벽한 바이브코딩 체계입니다!**

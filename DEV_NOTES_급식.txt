=== 🍽️ 급식 시스템 개발 노트 ===

## 🤖 스케줄러 시스템 정리 (2025-08-17)

### 🎯 **월장원 Champion Criteria 스케줄러**

#### **자동 실행 스케줄러** ✅ **활성화됨**
- **파일**: `.github/workflows/champion-criteria-scheduler.yml`
- **실행 시간**: 매월 6시 KST (UTC 21시)
- **기능**: 모든 학교의 해당 월 champion_criteria 데이터 자동 생성
- **API 키**: `ADMIN_API_KEY` 사용
- **호출 대상**: `/.netlify/functions/schedule-champion-criteria`

#### **1회성 실행 로직** ✅ **구현됨**
- **파일**: `apps/app/src/app/school-search/page.tsx` (197-218줄)
- **실행 시점**: 학생이 학교 등록/변경할 때 자동 실행
- **기능**: 해당 학교의 현재 달 champion_criteria 즉시 생성
- **호출 대상**: `/.netlify/functions/initialize-school-champion-criteria`
- **특징**: 실패해도 학교 등록은 계속 진행 (warn 처리)

### 🗑️ **삭제된 급식 자동 스케줄러** ❌ **제거됨**
- **삭제된 파일들**:
  - `.github/workflows/meal-scheduler.yml` (매일 오후 2시 2분 실행)
  - `src/pages/api/cron/meals.ts` (급식 정보 자동 업데이트 API)
  - `src/pages/api/cron/` 디렉토리
- **삭제 이유**: 현재 로직은 사용자 페이지 진입 시 DB 확인 → 없으면 NEIS API 호출 방식 사용
- **관련 환경변수**: `CRON_API_KEY` 삭제됨

### 🔧 **현재 급식 정보 로직**
```typescript
// apps/app/src/app/api/meals/route.ts
// 1. DB에서 급식 정보 먼저 확인
const { data: dbMeals } = await supabase
  .from('meal_menus')
  .select('*')
  .eq('school_code', schoolCode)
  .eq('meal_date', formattedDate);

// 2. DB에 있으면 바로 반환
if (dbMeals && dbMeals.length > 0) {
  return NextResponse.json({ meals: dbMeals, source: 'database' });
}

// 3. DB에 없으면 NEIS API 호출 후 저장
const mealData = await fetchMealInfo(schoolCode, officeCode, date);
```

### 📋 **환경변수 최적화 결과**
- ❌ **삭제됨**: `CRON_API_KEY`, `SECRETS_SCAN_SMART_DETECTION_ENABLED`, `SECRETS_SCAN_SMART_DETECTION_OMIT_VALUES`
- ✅ **추가됨**: `SECRETS_SCAN_SMART_DETECTION_ENABLED=false` (Firebase 스캔 문제 해결)
- ✅ **유지됨**: `ADMIN_API_KEY` (월장원 스케줄러용)

## 📋 핵심 시스템 구조

### 1️⃣ 급식 데이터 관리
- **NEIS API**: 교육청 급식 데이터 자동 수집
- **데이터베이스**: meals 테이블에 급식 정보 저장
- **주요 필드**: date, school_code, grade, meal_content

### 2️⃣ 급식 퀴즈 시스템
- **자동 생성**: 급식 메뉴 기반 퀴즈 자동 생성
- **정답 체크**: 사용자 답안과 실제 급식 메뉴 비교
- **결과 저장**: quiz_results 테이블에 정답/오답 기록

### 3️⃣ 급식 평점 시스템
- **개별 평점**: 메뉴별 1-5점 평점 입력
- **평점 집계**: 평균 평점 및 평점 개수 계산
- **배틀 시스템**: 일별/월별 메뉴 순위 결정

### 4️⃣ 급식 이미지 업로드
- **시간 제약**: 당일 12시~자정만 업로드 가능
- **AI 분석**: 업로드된 이미지 자동 분석
- **검증**: 실제 급식 메뉴와 일치 여부 확인

## 🗄️ 주요 데이터베이스 테이블

### meals (급식 정보)
```sql
CREATE TABLE meals (
  id SERIAL PRIMARY KEY,
  date DATE NOT NULL,
  school_code VARCHAR(20) NOT NULL,
  grade INTEGER NOT NULL,
  meal_content TEXT,
  created_at TIMESTAMP DEFAULT NOW()
);
```

### meal_quizzes (급식 퀴즈)
```sql
CREATE TABLE meal_quizzes (
  id SERIAL PRIMARY KEY,
  meal_date DATE NOT NULL,
  school_code VARCHAR(20) NOT NULL,
  grade INTEGER NOT NULL,
  quiz_content TEXT,
  correct_answer TEXT,
  created_at TIMESTAMP DEFAULT NOW()
);
```

### quiz_results (퀴즈 결과)
```sql
CREATE TABLE quiz_results (
  id SERIAL PRIMARY KEY,
  user_id UUID REFERENCES users(id),
  quiz_id INTEGER REFERENCES meal_quizzes(id),
  user_answer TEXT,
  is_correct BOOLEAN,
  submitted_at TIMESTAMP DEFAULT NOW()
);
```

## 🎯 장원 시스템 (급식 기반)

### 핵심 원칙
- **급식일수 기준**: 해당 주/월 실제 급식 제공일수와 정답수 완전 일치
- **실시간 체크**: 퀴즈 정답 시 즉시 장원 상태 업데이트
- **자동 처리**: 미제출 시 자동 실패 처리

### 주요 테이블

#### champion_criteria (급식일수 기준)
- `week_1_days` ~ `week_5_days`: 주차별 급식일수
- `month_total`: 월별 총 급식일수
- 학교/학년/년/월별로 관리

#### quiz_champions (사용자 정답 누적)
- `week_1_correct` ~ `week_5_correct`: 주차별 정답수
- `month_correct`: 월별 총 정답수
- 사용자별 실적 누적

### 장원 조건
- **주장원**: 해당 주 급식일수와 정답수 완전 일치
- **월장원**: 해당 월 급식일수와 정답수 완전 일치
- **결정 시점**: 매주 금요일 (급식 실제 발생 확인 후)

### 실시간 장원 체크
**파일**: `apps/app/src/utils/championCalculator.ts`
**함수**: `checkChampionStatusOnQuizSubmit(userId, schoolCode, grade, currentWeekNumber)`

```typescript
// 사용법
await championCalculator.checkChampionStatusOnQuizSubmit(
  userId, 
  schoolCode, 
  grade, 
  currentWeekNumber
);
```

## ⚙️ 시간 제약 관리

### 별점 시간 제약
**파일**: `apps/app/src/components/MealCard.tsx`
**함수**: `canRateAtCurrentTime`

#### 제약 해제 (테스트 모드)
```typescript
// 테스트용: 시간 제약 해제 (주석 해제하면 항상 허용)
return true;  // 이 줄의 주석만 해제!
```

### 파일 업로드 시간 제약
**파일**: `apps/app/src/components/MealImageUploader.tsx`
**제약**: 당일 12시~자정만 업로드 가능

#### 해제 방법
```typescript
// 테스트용: 시간 제약 해제
const isPastUploadCutoffTime = true; // 이 줄로 강제 설정
// const isPastUploadCutoffTime = hour >= 12; // 기존 로직 주석 처리
```

## ⚠️ 주의사항

### 운영 시 체크리스트
1. **시간 제약 활성화**: 모든 시간 제약 함수 정상 작동 확인
2. **데이터 동기화**: quiz_results와 quiz_champions 동기화 상태 확인
3. **급식 데이터**: NEIS API 정상 작동 및 데이터 수집 확인
4. **장원 시스템**: 실시간 장원 체크 정상 작동 확인

### 테스트 시 주의사항
- **운영 배포 전 반드시 제약 다시 활성화할 것**
- **테스트 완료 후 즉시 원복할 것**
- **커밋 시 제약 해제 상태로 푸시하지 말 것**

## 🔧 주요 기능 구현

### 1️⃣ 급식 데이터 수집
**파일**: `netlify/functions/fetch-meals.js`
- NEIS API 호출하여 급식 데이터 수집
- 학교별, 날짜별 급식 메뉴 저장
- 중복 데이터 방지 로직

### 2️⃣ 퀴즈 자동 생성
**파일**: `apps/app/src/app/quiz/QuizClient.tsx`
- 급식 메뉴 기반 객관식 퀴즈 생성
- AI를 활용한 선택지 생성
- 정답 검증 시스템

### 3️⃣ 평점 시스템
**파일**: `apps/app/src/components/MealCard.tsx`
- 메뉴별 별점 평가 기능
- 평균 평점 실시간 계산
- 평점 통계 표시

```javascript
// POST, DELETE 요청에서 배틀 계산 트리거 주석 처리
// await triggerBattleCalculation(mealId, schoolCode);
```

#### 2️⃣ 프론트엔드 실시간 구독 최적화
**파일**: `src/components/MyMealRating.tsx`
- meal_rating_stats 테이블 변경 감지 실시간 구독
- 디바운스된 배틀 계산 트리거 구현

```typescript
// meal_rating_stats 변경 감지 시 배틀 계산 트리거
const triggerBattleCalculation = useCallback(
  debounce(async () => {
    // 배틀 계산 로직
  }, 1000),
  [mealId]
);
```

#### 3️⃣ 데이터베이스 스키마 개선
**외래키 관계 설정**: meal_rating_stats.meal_id ↔ meal_menus.id

#### 4️⃣ 배틀 계삸 함수 최적화
**파일**: `src/utils/battleCalculator.ts`

##### 일별 급식 배틀 계산 (`calculateDailyMealBattle`)
```typescript
// 외래키 조인 방식으로 변경
let query = supabase
  .from('meal_rating_stats')
  .select(`
    school_code,
    avg_rating,
    rating_count,
    meal_menus!inner(
      meal_date
    )
  `)
  .eq('meal_menus.meal_date', date)
  .gt('rating_count', 0);
```

##### 월별 급식 배틀 계산 (`calculateMonthlyMealBattle`)
```typescript
// 외래키 조인 방식으로 변경
let query = supabase
  .from('meal_rating_stats')
  .select(`
    school_code,
    avg_rating,
    rating_count,
    meal_menus!inner(
      meal_date
    )
  `)
  .gte('meal_menus.meal_date', startDate)
  .lte('meal_menus.meal_date', endDate)
  .gt('rating_count', 0);
```

### 최적화 결과
✅ **중복 트리거 방지**: meal_rating_stats 변경 시에만 배틀 계산 실행
✅ **DB 오류 해결**: unique constraint 오류 제거
✅ **성능 향상**: 외래키 조인으로 효율적인 쿼리 실행
✅ **실시간 동기화**: 프론트엔드에서 실시간 배틀 계산 트리거

### 주의사항
- **외래키 관계 유지**: meal_rating_stats.meal_id ↔ meal_menus.id 관계 유지 필수
- **실시간 구독 모니터링**: Supabase Realtime 채널 오류 모니터링
- **디바운스 설정**: 배틀 계산 트리거 디바운스 시간 조정 가능

## 📋 핵심 시스템 구조

### 1️⃣ 급식 데이터 관리
- **NEIS API**: 교육청 급식 데이터 자동 수집
- **데이터베이스**: meals 테이블에 급식 정보 저장
- **주요 필드**: date, school_code, grade, meal_content

### 2️⃣ 급식 퀴즈 시스템
- **자동 생성**: 급식 메뉴 기반 퀴즈 자동 생성
- **정답 체크**: 사용자 답안과 실제 급식 메뉴 비교
- **결과 저장**: quiz_results 테이블에 정답/오답 기록

### 3️⃣ 급식 평점 시스템
- **개별 평점**: 메뉴별 1-5점 평점 입력
- **평점 집계**: 평균 평점 및 평점 개수 계산
- **배틀 시스템**: 일별/월별 메뉴 순위 결정

### 4️⃣ 급식 이미지 업로드
- **시간 제약**: 당일 12시~자정만 업로드 가능
- **AI 분석**: 업로드된 이미지 자동 분석
- **검증**: 실제 급식 메뉴와 일치 여부 확인

## 🗄️ 주요 데이터베이스 테이블

### meals (급식 정보)
```sql
CREATE TABLE meals (
  id SERIAL PRIMARY KEY,
  date DATE NOT NULL,
  school_code VARCHAR(20) NOT NULL,
  grade INTEGER NOT NULL,
  meal_content TEXT,
  created_at TIMESTAMP DEFAULT NOW()
);
```

### meal_quizzes (급식 퀴즈)
```sql
CREATE TABLE meal_quizzes (
  id SERIAL PRIMARY KEY,
  meal_date DATE NOT NULL,
  school_code VARCHAR(20) NOT NULL,
  grade INTEGER NOT NULL,
  quiz_content TEXT,
  correct_answer TEXT,
  created_at TIMESTAMP DEFAULT NOW()
);
```

### quiz_results (퀴즈 결과)
```sql
CREATE TABLE quiz_results (
  id SERIAL PRIMARY KEY,
  user_id UUID REFERENCES users(id),
  quiz_id INTEGER REFERENCES meal_quizzes(id),
  user_answer TEXT,
  is_correct BOOLEAN,
  submitted_at TIMESTAMP DEFAULT NOW()
);
```

## 🎯 장원 시스템 (급식 기반)

### 핵심 원칙
- **급식일수 기준**: 해당 주/월 실제 급식 제공일수와 정답수 완전 일치
- **실시간 체크**: 퀴즈 정답 시 즉시 장원 상태 업데이트
- **자동 처리**: 미제출 시 자동 실패 처리

### 주요 테이블

#### champion_criteria (급식일수 기준)
- `week_1_days` ~ `week_5_days`: 주차별 급식일수
- `month_total`: 월별 총 급식일수
- 학교/학년/년/월별로 관리

#### quiz_champions (사용자 정답 누적)
- `week_1_correct` ~ `week_5_correct`: 주차별 정답수
- `month_correct`: 월별 총 정답수
- 사용자별 실적 누적

### 장원 조건
- **주장원**: 해당 주 급식일수와 정답수 완전 일치
- **월장원**: 해당 월 급식일수와 정답수 완전 일치
- **결정 시점**: 매주 금요일 (급식 실제 발생 확인 후)

### 실시간 장원 체크
**파일**: `apps/app/src/utils/championCalculator.ts`
**함수**: `checkChampionStatusOnQuizSubmit(userId, schoolCode, grade, currentWeekNumber)`

```typescript
// 사용법
await championCalculator.checkChampionStatusOnQuizSubmit(
  userId, 
  schoolCode, 
  grade, 
  currentWeekNumber
);
```

## ⚙️ 시간 제약 관리

### 별점 시간 제약
**파일**: `apps/app/src/components/MealCard.tsx`
**함수**: `canRateAtCurrentTime`

#### 제약 해제 (테스트 모드)
```typescript
// 테스트용: 시간 제약 해제 (주석 해제하면 항상 허용)
return true;  // 이 줄의 주석만 해제!
```

### 파일 업로드 시간 제약
**파일**: `apps/app/src/components/MealImageUploader.tsx`
**제약**: 당일 12시~자정만 업로드 가능

#### 해제 방법
```typescript
// 테스트용: 시간 제약 해제
const isPastUploadCutoffTime = true; // 이 줄로 강제 설정
// const isPastUploadCutoffTime = hour >= 12; // 기존 로직 주석 처리
```

## ⚠️ 주의사항

### 운영 시 체크리스트
1. **시간 제약 활성화**: 모든 시간 제약 함수 정상 작동 확인
2. **데이터 동기화**: quiz_results와 quiz_champions 동기화 상태 확인
3. **급식 데이터**: NEIS API 정상 작동 및 데이터 수집 확인
4. **장원 시스템**: 실시간 장원 체크 정상 작동 확인

### 테스트 시 주의사항
- **운영 배포 전 반드시 제약 다시 활성화할 것**
- **테스트 완료 후 즉시 원복할 것**
- **커밋 시 제약 해제 상태로 푸시하지 말 것**

## 📝 4:55 푸시 이후 사용자 요구사항

### 1️⃣ Champion History 시스템 단순화 목표
- **목적**: grade 구분 제거, school 레벨에서만 champion 운영
- **현재 문제**: ChampionHistory 컴포넌트 무한 로딩 또는 에러
- **필요 작업**: 초기화부터 차근차근 시작

### 2️⃣ 사용자가 강조한 핵심 원칙
- **절대 금지**: 기존 작동하는 기능 건드리지 말 것
- **과거 경험**: 동일한 Netlify 문제로 일주일 고생한 이력 있음
- **접근법**: 한 번에 하나씩, 단계별 승인 프롬프트개선16

### 3️⃣ 사용자 불만사항
- "맥락을 이어가지 못하는 치매환자 같은 작업"
- "기본적이고 가장 중요한 기능 다 망쳐놨다"
- "과거 같은 문제 반복하는 것에 대한 강한 불만"

## 🎯 다음 단계 계획

### Phase 1: Champion History 문제 정확한 파악
1. 현재 ChampionHistory 컴포넌트 상태 확인
2. 어떤 에러가 발생하는지 정확히 파악
3. grade 관련 코드가 남아있는지 체크

### Phase 2: 최소한의 수정으로 해결
1. Champion History 관련 코드만 수정
2. 다른 기능은 절대 건드리지 않음
3. 각 수정마다 사용자 승인 받기

### Phase 3: 단계별 검증
1. 수정 후 즉시 테스트
2. 기본 기능 영향 없는지 확인
3. Champion History 정상 작동 확인

## ⚠️ 절대 원칙 (다시는 어기지 말 것)
1. **netlify.toml 절대 건드리지 않기** - 현재 잘 작동함
2. **기존 작동 기능 보호** - 퀴즈, 급식 등
3. **사용자 경고 진지하게 받아들이기**

## 현재 해야 할 일
- Champion History 페이지 상태 정확히 파악

=== 테스트용 시간 제약 해제 방법 (2025-07-11) ===

## 별점 시간 제약 해제
파일: apps/app/src/components/MealCard.tsx
함수: canRateAtCurrentTime

### 제약 해제 (테스트 모드)
```typescript
// 테스트용: 시간 제약 해제 (주석 해제하면 항상 허용)
return true;  // 이 줄의 주석만 해제!
```

### 제약 활성화 (운영 모드)
```typescript
// 테스트용: 시간 제약 해제 (주석 해제하면 항상 허용)
// return true;  // 이 줄을 주석 처리!
```

## 📍 다른 시간 제약 조건들

### 1️⃣ 파일 업로드 (MealImageUploader)
- **위치**: `apps/app/src/components/MealImageUploader.tsx`
- **함수**: `checkIfAiImageNeeded` 내에서 시간 체크
- **제약**: 당일 12시~자정만 업로드 가능
- **해제 방법**: `isPastUploadCutoffTime` 조건을 `true`로 강제 설정
- **예시**: 
  ```typescript
  // 테스트용: 시간 제약 해제
  const isPastUploadCutoffTime = true; // 이 줄로 강제 설정
  // const isPastUploadCutoffTime = hour >= 12; // 기존 로직 주석 처리
  ```

### 2️⃣ 퀴즈 생성
- **위치**: `apps/app/src/app/quiz/QuizClient.tsx`
- **상태**: ✅ **시간 제약 없음** - 언제든지 퀴즈 생성 가능
- **확인 완료**: `handleManualQuizGenerate` 함수에 시간 제약 로직 없음

### 3️⃣ 배틀 기능
- **상태**: ⚠️ **미구현** - 아직 배틀 관련 컴포넌트 없음
- **추후 구현 시**: 시간 제약 필요 여부 검토 예정

---

=== 🔒 이미지 검증 시스템 개선 (2025-07-29) ===

## 문제점 분석
**파일**: `apps/app/netlify/functions/verify-meal-image.js`

### 심각한 보안 취약점 발견
- **이미지 URL 접근 실패 시**: 자동으로 `approved` 처리
- **OpenAI API 오류/타임아웃 시**: 자동으로 `approved` 처리
- **네트워크 문제 시**: 검증 없이 승인되는 치명적 결함

### 기존 문제 코드
```javascript
// 문제: 예외 발생 시 자동 승인
try {
  // 이미지 다운로드 및 OpenAI 검증
  const response = await openai.chat.completions.create(...);
  // ... 검증 로직
} catch (error) {
  console.error('검증 중 오류:', error);
  // 🚨 문제: 오류 시 자동 승인!
  return {
    statusCode: 200,
    body: JSON.stringify({
      success: true,
      result: 'approved',
      message: '이미지가 승인되었습니다.'
    })
  };
}
```

## 수정 내용

### 1️⃣ 오류 시 자동 거부 로직 구현
```javascript
// 수정: 예외 발생 시 명확한 거부 처리
try {
  // 이미지 다운로드 시도
  const imageResponse = await fetch(imageUrl);
  if (!imageResponse.ok) {
    throw new Error(`이미지 다운로드 실패: ${imageResponse.status}`);
  }
  
  // OpenAI API 호출
  const response = await openai.chat.completions.create({
    model: "gpt-4-vision-preview",
    messages: [...],
    max_tokens: 300,
    timeout: 30000  // 30초 타임아웃
  });
  
  // 정상 검증 로직...
  
} catch (error) {
  console.error('🚨 이미지 검증 실패:', error);
  
  // 🔒 보안 강화: 오류 시 무조건 거부
  return {
    statusCode: 200,
    body: JSON.stringify({
      success: true,
      result: 'rejected',
      message: '이미지 검증 중 오류가 발생했습니다.',
      reason: '시스템 오류로 인한 자동 거부',
      details: error.message
    })
  };
}
```

### 2️⃣ 상세한 거부 사유 분류
```javascript
// 거부 사유별 명확한 메시지
const rejectionReasons = {
  NETWORK_ERROR: '네트워크 오류로 인한 검증 실패',
  IMAGE_ACCESS_FAILED: '이미지 접근 불가',
  OPENAI_TIMEOUT: 'AI 분석 시간 초과',
  OPENAI_ERROR: 'AI 분석 서비스 오류',
  INVALID_RESPONSE: 'AI 응답 형식 오류'
};

// 오류 타입별 처리
if (error.code === 'ENOTFOUND' || error.code === 'ECONNREFUSED') {
  reason = rejectionReasons.NETWORK_ERROR;
} else if (error.message.includes('timeout')) {
  reason = rejectionReasons.OPENAI_TIMEOUT;
} else if (error.message.includes('이미지 다운로드')) {
  reason = rejectionReasons.IMAGE_ACCESS_FAILED;
} else {
  reason = rejectionReasons.OPENAI_ERROR;
}
```

### 3️⃣ 이미지 크기 최적화 확인
**파일**: `apps/app/src/components/MealImageUploader.tsx`

**기존 구현 상태 확인**:
✅ **이미지 리사이징 이미 구현됨**
- 최대 크기: 1024px × 1024px
- 압축 품질: JPEG 85%
- OpenAI API 타임아웃 방지 목적

=== 📱 급식 공유 기능 & 추천 시스템 (2025-08-04) ===
    const canvas = document.createElement('canvas');
    const ctx = canvas.getContext('2d')!;
    const img = new Image();
    
    img.onload = () => {
      const maxSize = 1024;
      let { width, height } = img;
      
      // 비율 유지하면서 크기 조정
      if (width > height) {
        if (width > maxSize) {
          height = (height * maxSize) / width;
          width = maxSize;
        }
      } else {
        if (height > maxSize) {
          width = (width * maxSize) / height;
          height = maxSize;
        }
      }
      
      canvas.width = width;
      canvas.height = height;
      ctx.drawImage(img, 0, 0, width, height);
      
      // JPEG 85% 품질로 압축
      resolve(canvas.toDataURL('image/jpeg', 0.85));
    };
    
    img.src = URL.createObjectURL(file);
  });
};
```

## 보안 개선 결과

### ✅ 달성된 보안 강화
1. **무분별한 자동 승인 방지**: 오류 시 무조건 거부
2. **명확한 거부 사유**: 사용자에게 구체적인 실패 원인 제공
3. **로깅 강화**: 모든 검증 실패 케이스 상세 기록
4. **타임아웃 설정**: OpenAI API 30초 타임아웃으로 무한 대기 방지

### ✅ 사용자 경험 개선
1. **투명한 피드백**: 거부 시 명확한 이유 제공
2. **빠른 응답**: 타임아웃으로 긴 대기 시간 방지
3. **안정적인 서비스**: 네트워크 오류에도 적절한 응답

### ✅ 운영 안정성 확보
1. **예외 상황 대응**: 모든 오류 케이스에 대한 명확한 처리
2. **디버깅 용이성**: 상세한 오류 로그로 문제 추적 가능
3. **보안 정책 준수**: "의심스러우면 거부" 원칙 적용

## 📝 핵심 교훈
1. **보안 우선 원칙**: 불확실한 상황에서는 항상 안전한 선택 (거부)
2. **사용자 피드백**: 단순한 성공/실패가 아닌 구체적인 이유 제공
3. **오류 처리의 중요성**: 예외 상황이 보안 취약점이 되지 않도록 주의
4. **성능과 보안의 균형**: 이미지 최적화로 API 안정성과 사용자 경험 모두 확보

## 🔧 테스트용 시간 제약 해제 방법

### 공통 해제 방법
**모든 시간 제약 함수에서 동일한 패턴 사용:**
1. 함수 맨 앞에 `return true;` 추가 (주석 처리)
2. 테스트 시: 주석 해제
3. 운영 시: 다시 주석 처리

## ⚠️ 주의사항
- **운영 배포 전 반드시 제약 다시 활성화할 것**
- **테스트 완료 후 즉시 원복할 것**
- **커밋 시 제약 해제 상태로 푸시하지 말 것**
- 무엇이 문제인지 사용자와 확인
- 최소한의 변경으로 해결책 제시

=== 기존 개발 노트 ===

출시전 조건 (퀴즈 -> 당일 퀴즈풀이만 가능하게
루트에 있는 파일들 점검 후 제거 
netli / functions   - cron-meals.js
netli / functions   - manual-generate-meal-quiz.js

=== 장원 시스템 자동화 (2025년 7월 5일) ===

## 1. 자동화 구조 개요

### 1.1 핵심 기능
- **NEIS 급식 데이터 기반 장원 조건 자동 계산**
- **실시간 장원 상태 업데이트 (퀴즈 제출 시)**
- **미제출 자동 실패 처리 (마감 시간 이후)**
- **신규 학교 자동 등록 지원**

### 1.2 시스템 구성요소
- **신규 테이블**: `champion_criteria`, `user_champion_records`
- **Netlify Functions**: 
  - `initialize-champion-criteria.js`: 초기 데이터 설정 (6-8월)
  - `monthly-champion-criteria.js`: 매월 1일 다음 달 급식일 데이터 수집
  - `update-champion-status-on-quiz-submit.js`: 퀴즈 제출 시 실시간 상태 업데이트
  - `mark-missing-submissions-failed.js`: 금요일 마감후 미제출자 실패 처리


### 1.3 환경 변수
- `CHAMPION_TEST_MODE`: 테스트 모드 활성화 ('true'/'false')
- `USE_NEW_CHAMPION_SYSTEM`: 새 시스템 활성화 ('true'/'false')

## 2. 새 장원 시스템 작동 방식

### 2.1 장원 조건 설정 프로세스
1. **초기 설정**: `initialize-champion-criteria` 함수로 테스트용 6-8월 데이터 일괄 설정
2. **월간 업데이트**: 매월 1일 자동으로 다음 달의 장원 조건 계산 및 저장
3. **학교별 처리**: 각 학교와 학년별로 주차별/월별 급식 일수 계산

### 2.2 장원 상태 결정 프로세스
1. **실시간 업데이트**: 
   - 퀴즈 제출 시마다 즉시 장원 상태 계산 (정답→진행중, 오답→실패)
   - 필요 정답 수 충족 시 즉시 장원으로 표시
2. **자동 실패 처리**: 
   - 금요일 오후 1시 10분 실행
   - 퀴즈 미제출 사용자를 자동으로 실패로 표시
3. **기존 시스템과 병행**: 
   - 두 시스템 동시 운영으로 안정성 확보
   - 기존 `quiz_champion_history` 테이블도 함께 업데이트

### 2.3 테스트 모드 지원
- `CHAMPION_TEST_MODE=true` 설정 시:
  - 과거 기간에 대한 퀴즈 제출 허용
  - 자동 실패 처리 비활성화
  - 테스트 데이터와 실제 데이터 구분 가능

## 3. 주요 스케줄러 설정
- 매월 1일 01:00 - 다음 달 장원 조건 설정
- 매주 금요일 13:10 - 미제출자 자동 실패 처리
- 기존 스케줄러 유지 (주간/월간 집계)

## 4. 초기화 범위 및 설정

### 4.1 초기화 함수 범위
- **initialize-champion-criteria**: 모든 학교의 6-8월 데이터 일괄 초기화

### 4.2 학교 등록 훅 초기화 범위
- **현재 달**: 현재 진행 중인 월의 모든 주차 (1-5주)
- **다음 달**: 다음 달의 모든 주차 (1-5주)
- **이전 달**: 테스트 모드에서만 지난 달 데이터 추가 초기화 (TEST_MODE=true인 경우)

### 4.3 코드 내 테스트 설정
```javascript
// 각 함수 상단에 상수로 설정
const TEST_MODE = true;      // 테스트용, 출시 전 false로 변경
const USE_NEW_SYSTEM = false; // 테스트 완료 후 true로 변경
```

## 5. 배포 및 테스트 가이드
1. 코드 상태: TEST_MODE=true, USE_NEW_SYSTEM=false로 유지한 채 배포
2. **초기화 방법**: 
   - 처음 한 번: initialize-champion-criteria.js 함수를 URL로 접속해 실행
   - 신규 학교 등록 시: **initialize-school-champion-criteria.js가 자동으로 실행되어 크리테리아 설정**
   - **1회성 강제 업데이트**: force-june-champion-criteria.js (특정 월 전체 학교 재계산용)
     * 사용법: `/.netlify/functions/force-june-champion-criteria` URL 접속
     * 기능: 모든 학교의 특정 월 크리테리아를 NEIS API 기반으로 강제 재계산
     * 특징: 기존 데이터 삭제 → 실제 급식일수 조회 → 주차별 계산 → 저장
3. 테스트: 실제 UI로 퀴즈를 풀면서 새 장원 시스템 동작 확인
4. 전환 방법: 
   - 코드 내 USE_NEW_SYSTEM=true로 변경 후 재배포
   - 최종 출시 전 TEST_MODE=false로 변경 후 배포

=== 주장원/월장원 시스템 구현  REDESIGNING (2025년 7월 5일 재설계)
===## 주장원/월장원 시스템 구현 🔄 REDESIGNING (2025년 7월 5일 재설계)

### 📋 **정확한 장원 조건 정의**

#### 🏆 **주장원 조건** (ISO 기준)
- **주차 기준**: "각 주차는 '해당 주의 월요일이 속한 달'을 기준으로 묶어 ○월 n주 장원으로 명명"
- **급식일수 조건**: 해당 주 학교별 실제 급식 제공일수와 유저 정답수가 **완전 일치**
- **결정 시점**: 매주 금요일 (급식 실제 발생 확인 + 마지막 퀴즈 완료 후)
- **예시**: 7월 2주차에 급식 3일 제공 → 3일 모두 정답 → 주장원

#### 👑 **월장원 조건**
- **급식일수 조건**: 해당월 학교별 실제 급식 제공 총일수와 유저 정답수가 **완전 일치**
- **결정 시점**: 매월 마지막날 최종 집계
- **예시**: 7월 총 급식 15일 제공 → 15일 모두 정답 → 월장원

### 🗃️ **새로운 DB 스키마** (기존 테이블과 완전 독립)
```sql
CREATE TABLE quiz_champion_statistics (
  id BIGSERIAL PRIMARY KEY,
  user_id UUID REFERENCES auth.users(id),
  school_id BIGINT REFERENCES schools(id),
  
  -- 기간 정보 (ISO 기준)
  year INTEGER NOT NULL,
  month INTEGER NOT NULL, 
  week_number INTEGER, -- 주장원용 (1-6주)
  period_type TEXT NOT NULL CHECK (period_type IN ('weekly', 'monthly')),
  
  -- 급식/퀴즈 통계
  total_meal_days INTEGER NOT NULL,    -- 해당 기간 실제 급식 제공일수
  total_quiz_days INTEGER NOT NULL,    -- 해당 기간 퀴즈 출제일수
  correct_answers INTEGER NOT NULL,    -- 유저 정답수
  
  -- 장원 결과
  is_champion BOOLEAN DEFAULT FALSE,   -- 장원 여부
  determined_at TIMESTAMP,             -- 결정 시점
  
  created_at TIMESTAMP DEFAULT NOW(),
  UNIQUE(user_id, school_id, year, month, week_number, period_type)
);
===

### 탈퇴 문제 해결 (2025년 7월 3일)

#### 문제 상황
- UI/UX상 탈퇴처리가 된 듯하나 DB와 Authentication에 사용자 데이터 남아있음

#### 기존 로직 문제점
1. DB에서 users 테이블만 삭제
2. 관련 데이터 (quiz_results, quiz_champions) 미삭제
3. Supabase Auth에서 사용자 계정 미삭제
4. 세션 로그아웃만 수행

#### 해결 방안 구현
1. **관련 데이터 삭제**: quiz_results, quiz_champions 순서대로 삭제
2. **users 테이블 삭제**: 기존 로직 유지
3. **Auth 사용자 삭제**: admin.deleteUser() 사용
4. **세션 로그아웃**: 기존 로직 유지

#### 구현 완료 사항
- ✅ supabase-server.ts에 createAdminClient() 추가
- ✅ delete-account API에 완전한 삭제 로직 구현
- ✅ 외래키 관계 순서대로 데이터 삭제

#### 필요한 설정
⚠️ **중요**: .env 파일에 SUPABASE_SERVICE_ROLE_KEY 추가 필요
```
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here
```

#### 테스트 시나리오
1. 탈퇴 버튼 클릭
2. 확인 대화상자 승인
3. 로딩 상태 확인
4. 로그아웃 되어 메인페이지로 이동
5. 로그인 시도시 사용자 없음 오류 확인
6. Supabase Dashboard에서 Authentication 및 Database 완전 삭제 확인


장원(Champion) 시스템 프로세스 설명
핵심 테이블 및 필드
1. quiz_champion_history 테이블
주요 필드:
id, user_id, school_code, grade, year, month, week_number
period_type ('weekly' 또는 'monthly')
total_meal_days (급식 제공 일수)
total_count (퀴즈 출제 일수)
correct_count (정답 수)
accuracy_rate (정확도)
avg_answer_time (평균 응답 시간)
is_champion (장원 여부)
is_current (현재 상태 여부)
2. meals 테이블
주요 필드: date, school_code, grade (급식 정보)
3. quiz_results 테이블
주요 필드: user_id, date, school_code, grade, is_correct, answer_time
프로세스 흐름
1. 클라이언트 요청 (ChampionHistory.tsx)
사용자가 ChampionHistory 컴포넌트를 로드하면:
주간/월간 통계 요청을 GET /api/champion/calculate로 전송
URLSearchParams로 user_id, school_code, grade, year, month, week_number, period_type 파라미터 전달
2. API 처리 (route.ts)
GET 요청 처리:
URL 쿼리 파라미터 파싱
DB에서 기존 통계 조회
통계가 없으면 자동 계산 및 저장
POST 요청 처리:
본문에서 파라미터 추출
통계 계산 및 저장
3. 통계 계산 (championCalculator.ts)
주간 통계 계산:
getWeekInfoByWeekNumber
: 주어진 year, month, week_number로 주 시작일/종료일 계산
calculateMealDays
: 해당 주의 급식 일수 계산 (meals 테이블 사용)
getQuizResults
: 해당 주의 퀴즈 결과 조회 (quiz_results 테이블 사용)
장원 결정: 급식일수 = 정답수 조건 확인
월간 통계 계산:
해당 월의 시작일/종료일 계산
월별 급식 일수 및 퀴즈 결과 조회
장원 결정
통계 저장:
saveStatistics
: 계산된 통계를 quiz_champion_history 테이블에 저장
중복 키 충돌 시 upsert로 갱신
4. 클라이언트 응답 처리
API 응답 데이터를 상태에 저장
필드명 매핑 처리 (correct_count → correct_answers)
주간/월간 장원 정보 표시
이슈 해결
DB는 correct_count 필드를 사용하지만 클라이언트는 correct_answers 필드 참조
API 응답에서 필드 매핑을 추가하여 호환성 유지
getWeekInfoByWeekNumber
 함수에 유효성 검사 추가로 존재하지 않는 주차 요청 시 안전 처리
이 시스템은 사용자의 퀴즈 결과와 급식 데이터를 기반으로 주간/월간 장원을 계산하고 그 결과를 히스토리로 저장/표시합니다.

=== Netlify 함수 404 문제 해결 가이드 (2025-07-07) ===

문제 증상: 
- 함수가 배포되었지만 호출 시 404 Not Found 오류 발생
- 배포 로그에 "Site deploy was successfully initiated" 메시지 표시됨

원인 파악 방법:
1. 배포 로그 확인 필수! (가장 중요)
   - 함수 번들링 오류 확인 (주석, 중괄호 등 구문 오류)
   - "Failed to bundle functions" 메시지 검색
   - 폴백 번들러 사용 여부 확인

2. Netlify의 특성 이해:
   - 웹사이트와 함수는 별도 프로세스로 배포됨
   - 함수에 오류가 있어도 사이트 배포는 "성공"으로 표시됨
   - 일부 함수의 오류가 전체 함수 시스템에 영향을 줄 수 있음

해결 방법:
1. 배포 로그에서 구문 오류 수정
2. 모든 함수 파일의 주석, 중괄호 짝 맞추기
3. 수정 후 재배포
4. 함수 로그 확인하여 런타임 오류 확인

중요: Netlify에서 "배포 성공" 메시지는 함수가 정상 작동한다는 보장이 아님! 
항상 배포 로그의 오류 메시지를 꼼꼼히 확인할 것.

[2025-07-07] Netlify 함수 404 오류 해결 내용
- 문제: Netlify 서버리스 함수들이 404 오류를 반환하는 문제 발생
- 원인: 
  1. 일부 함수 파일에 `exports.handler` 래퍼 누락
  2. 루트 레벨과 앱 레벨에 중복된 함수 파일 존재
  3. 중복된 netlify.toml 설정 파일 충돌
  4. 함수 URL 접근 시 사용자 지정 도메인(lunbat.com) 사용 필요

- 해결 방법:
  1. 누락된 `exports.handler` 래퍼 추가 (update-meals.js, auto-generate-meal-images.js, check-missing-meal-images.js)
  2. 루트 레벨 함수 파일들을 백업 이름으로 변경 (.backup.js)
  3. apps/app/netlify.toml 파일을 .backup으로 변경하여 루트 설정만 사용
  4. 함수 URL 접근 시 올바른 도메인 사용: https://lunbat.com/.netlify/functions/[function-name]

- 테스트:
  - schools 함수: https://lunbat.com/.netlify/functions/schools?keyword=서울 (200 OK)
  - update-meals 함수: https://lunbat.com/.netlify/functions/update-meals (401 Unauthorized, 인증 필요하므로 정상)

- 주의사항:
  1. 함수 URL은 항상 사용자 지정 도메인(lunbat.com)으로 접근해야 함
  2. 대부분의 함수는 쿼리 파라미터나 인증이 필요함
  3. 함수 코드 수정 시 항상 exports.handler 구조 유지할 것

  [AI이미지생성] 버튼 활성/비활성화  조건
  meal_menus 테이블 조회
조건: school_code = schoolCode AND meal_date = 오늘날짜(한국시간)
결과: id (= mealId) 획득
저장: currentMealId 상태 변수에 저장
meal_images 테이블 조회
조건: meal_id = currentMealId AND status = 'approved'
결과 확인: 승인된 이미지 존재 여부 확인
버튼 활성화 조건: 승인된 이미지가 없을 때 + 12:30 이후(한국시간) + 유효한 메뉴 존재

=== 실시간 급식 평점 업데이트 문제 해결 (2025-07-12) ===

## 문제 상황
- 사용자가 메뉴 아이템 별점을 변경해도 급식 전체 평점이 실시간으로 UI에 반영되지 않음
- 페이지 새로고침 후에만 업데이트된 평점이 표시됨
- DB에는 정상적으로 데이터가 저장되고 있었음

## 원인 분석
- Supabase 실시간 구독 필터 구문 문제
- `meal_ratings` 테이블 구독 시 복합 필터 조건(`meal_id=eq.${mealId}&user_id=eq.${user.id}`)이 올바르게 작동하지 않음

## 해결 방법
1. **필터 구문 수정**:
   - 변경 전: `meal_id=eq.${mealId}&user_id=eq.${user.id}`
   - 변경 후: `meal_id=eq.${mealId}` (단일 필터로 단순화)

2. **코드 내 추가 검증**:
   ```typescript
   if (payload.new && 
       typeof payload.new === 'object' && 
       'user_id' in payload.new && 
       payload.new.user_id === user.id && 
       'rating' in payload.new) {
     setMyRating(payload.new.rating as number);
   }
   ```

3. **디버깅 코드 정리**:
   - 불필요한 로그 및 디버깅 코드 제거
   - 코드 가독성 및 성능 개선

## 기술적 배경
- Supabase 실시간 API에서 복합 필터 조건(`&`)이 예상대로 작동하지 않음
- 필터를 단순화하고 코드 내에서 추가 검증을 수행하는 방식이 더 안정적
- 이벤트는 수신되었지만 처리 로직에 문제가 있었음

## 결과
- 사용자가 메뉴 아이템 별점을 변경하면 급식 전체 평점이 실시간으로 UI에 반영됨
- 페이지 새로고침 없이도 평점 업데이트가 즉시 표시됨
- 불필요한 재계산이나 무한 루프 없이 안정적으로 작동

=== 퀴즈 챔피언 데이터 동기화 및 API 수정 (2025-07-13) ===

## 문제 상황
1. **데이터 불일치**: `quiz_results`와 `quiz_champions` 테이블 간 동기화 문제
   - 캘린더 UI에서 7/8개 정답으로 표시되지만 일부 날짜(7월 11일) 누락
   - `day_11` 필드가 NULL인데 실제로는 정답이어야 함

2. **챔피언 API 500 에러**: 주별/월별 장원 통계 조회 시 서버 에러 발생
   - "주 1 API 응답 실패: 500", "월별 API 응답 실패: 500" 에러

## 원인 분석
1. **데이터 동기화 문제**:
   - `quiz_results`에는 7월 11일 TRUE(정답) 데이터 존재
   - `quiz_champions`에서 `day_11` 필드가 NULL로 업데이트되지 않음
   - 동기화 SQL 쿼리에서 일부 날짜 필드 누락

2. **API 500 에러 원인**:
   - `/api/champion/calculate/route.ts`에서 `grade` 변수 사용하지만 정의되지 않음
   - 로그 출력 시 `grade: parseInt(grade)` 참조로 인한 ReferenceError
   - 이전에 `grade` 필드를 제거했지만 일부 코드에서 여전히 참조

## 해결 방법

### 1. 데이터 동기화 수정
```sql
-- 2025년 7월 데이터 완전 재동기화
DELETE FROM quiz_champions WHERE year = 2025 AND month = 7;

WITH quiz_data AS (
    SELECT 
        qr.user_id,
        EXTRACT(YEAR FROM mq.meal_date) as year,
        EXTRACT(MONTH FROM mq.meal_date) as month,
        COUNT(*) as total_count,
        SUM(CASE WHEN qr.is_correct THEN 1 ELSE 0 END) as month_correct,
        -- day_1부터 day_13까지 모든 날짜 필드 포함
        MAX(CASE WHEN EXTRACT(DAY FROM mq.meal_date) = 1 THEN (CASE WHEN qr.is_correct THEN 'O' ELSE 'X' END) END) as day_1,
        MAX(CASE WHEN EXTRACT(DAY FROM mq.meal_date) = 2 THEN (CASE WHEN qr.is_correct THEN 'O' ELSE 'X' END) END) as day_2,
        -- ... (중략)
        MAX(CASE WHEN EXTRACT(DAY FROM mq.meal_date) = 11 THEN (CASE WHEN qr.is_correct THEN 'O' ELSE 'X' END) END) as day_11,
        MAX(CASE WHEN EXTRACT(DAY FROM mq.meal_date) = 13 THEN (CASE WHEN qr.is_correct THEN 'O' ELSE 'X' END) END) as day_13
    FROM quiz_results qr
    JOIN meal_quizzes mq ON qr.quiz_id = mq.id
    WHERE EXTRACT(YEAR FROM mq.meal_date) = 2025 AND EXTRACT(MONTH FROM mq.meal_date) = 7
    GROUP BY qr.user_id, EXTRACT(YEAR FROM mq.meal_date), EXTRACT(MONTH FROM mq.meal_date)
)
INSERT INTO quiz_champions (...) SELECT ... FROM quiz_data;
```

### 2. 챔피언 API 수정
**파일**: `apps/app/src/app/api/champion/calculate/route.ts`

**수정 내용**:
1. GET 함수에서 `grade` 파라미터 참조 제거
2. 로그 출력에서 `grade` 정보 제거
3. 필수 파라미터 검증에서 `grade` 제거

```typescript
// 수정 전
const grade = searchParams.get('grade')
console.log('🔍 장원 통계 조회 API 호출:', {
  user_id, school_code, grade, year, month, week_number, period_type
})
if (!user_id || !school_code || !grade || !year || !month) {

// 수정 후  
console.log('🔍 장원 통계 조회 API 호출:', {
  user_id, school_code, year, month, week_number, period_type
})
if (!user_id || !school_code || !year || !month) {
```

## 결과
1. **데이터 동기화 완료**:
   - ✅ 캘린더 UI에서 7월 11일 정답 표시 정상화
   - ✅ `day_11` 필드가 'O'로 올바르게 업데이트됨
   - ✅ 월별 통계 (7/8개 정답) 정확히 표시

2. **챔피언 API 수정 완료**:
   - ✅ 500 에러 해결로 주별/월별 장원 통계 정상 조회
   - ✅ 주장원, 월장원 정보 UI에서 정상 표시
   - ✅ `grade` 필드 완전 제거로 코드 일관성 확보

## 기술적 배경
- **quiz_champions 테이블**: 월별 퀴즈 통계를 `day_1`~`day_31` 필드로 일별 결과 저장
- **동기화 전략**: `quiz_results` 기준으로 완전 재계산 후 `quiz_champions` 업데이트
- **API 아키텍처**: Next.js App Router 기반 서버리스 함수로 챔피언 통계 제공
- **에러 처리**: 파라미터 검증 및 예외 처리로 안정성 확보

## 주의사항
- 데이터 동기화 시 기존 데이터 삭제 후 재생성하므로 백업 권장
- `grade` 필드 관련 코드가 다른 곳에 남아있는지 주기적 점검 필요
- 챔피언 통계 계산 로직의 성능 최적화 고려 (대용량 데이터 시)

## 🔘 버튼 활성화 조건 및 시간 제약 관리

### 1️⃣ AI 이미지 생성 버튼 활성화 조건
**파일**: `apps/app/src/components/MealImageUploader.tsx`

**함수**: `checkIfAiImageNeeded`

#### 활성화 조건 (모두 만족해야 함)
1. **메뉴 존재 확인**
   - `meal_menus` 테이블에서 `school_code = schoolCode AND meal_date = 오늘날짜(한국시간)` 조건으로 조회
   - 유효한 `mealId` 획득 필요
   - `menu_items` 배열이 존재하고 "급식 정보가 없습니다" 메시지가 없어야 함

2. **당일 조건**
   - `mealDate === today` (한국시간 기준)

3. **시간 조건**
   - 오후 12:30 이후 (한국시간)
   - `isPastAiCutoffTime = hour > 12 || (hour === 12 && minute >= 30)`

### 2️⃣ 사진 업로드 버튼 활성화 조건
**파일**: `apps/app/src/components/MealImageUploader.tsx`

**함수**: `checkIfAiImageNeeded`

#### 활성화 조건
1. **메뉴 존재**: AI 이미지 생성 버튼과 동일
2. **당일 조건**: AI 이미지 생성 버튼과 동일
3. **시간 조건**
   - 오후 12:00 이후 (한국시간)
   - `isPastCutoffTime = hour >= 12`

### 3️⃣ 별점 시간 제약
**파일**: `apps/app/src/components/MealCard.tsx`

**함수**: `canRateAtCurrentTime`

#### 현재 상태
- **테스트 모드 활성화됨**: `return true;`로 하드코딩
- **원래 로직**: 당일 + 12시 이후만 별점 가능

### 4️⃣ 테스트 모드 전환 방법

#### 별점 시간 제약 해제/복원
**파일**: `apps/app/src/components/MealCard.tsx`
```typescript
// 테스트 모드 (시간 제약 해제)
const canRateAtCurrentTime = (mealDate: string): boolean => {
  return true; // 이 줄로 테스트 모드
  // 아래 로직들은 주석 처리됨
};

// 프로덕션 모드 (시간 제약 활성화)
const canRateAtCurrentTime = (mealDate: string): boolean => {
  // return true; // 이 줄을 주석 처리
  
  const now = new Date();
  // ... 실제 시간 체크 로직
};
```

#### 이미지 업로드 시간 제약 해제/복원
**파일**: `apps/app/src/components/MealImageUploader.tsx`

**테스트 모드 설정**:
```typescript
// 3. 시간 조건 확인 (테스트 모드)
const isPastCutoffTime = true; // 강제 활성화
// const isPastCutoffTime = hour >= 12; // 원래 로직 주석 처리

// AI 이미지 생성 버튼은 12:30 이후부터 활성화 (테스트 모드)
isPastAiCutoffTime = true; // 강제 활성화
// isPastAiCutoffTime = hour > 12 || (hour === 12 && minute >= 30); // 원래 로직 주석 처리
```

**프로덕션 모드 복원**:
```typescript
// 3. 시간 조건 확인 (프로덕션 모드)
// const isPastCutoffTime = true; // 테스트 모드 주석 처리
const isPastCutoffTime = hour >= 12; // 원래 로직 활성화

// AI 이미지 생성 버튼은 12:30 이후부터 활성화 (프로덕션 모드)
// isPastAiCutoffTime = true; // 테스트 모드 주석 처리
isPastAiCutoffTime = hour > 12 || (hour === 12 && minute >= 30); // 원래 로직 활성화
```

### ⚠️ 배포 전 체크리스트
- [ ] `canRateAtCurrentTime` 함수에서 `return true;` 주석 처리
- [ ] `MealImageUploader.tsx`에서 시간 제약 로직 원복
- [ ] 테스트 모드 관련 하드코딩 제거 확인
- [ ] 실제 시간 조건으로 버튼 동작 테스트

## 주의사항
- 데이터 동기화 시 기존 데이터 삭제 후 재생성하므로 백업 권장
- `grade` 필드 관련 코드가 다른 곳에 남아있는지 주기적 점검 필요
- 챔피언 통계 계산 로직의 성능 최적화 고려 (대용량 데이터 시)
- **시간 제약 테스트 모드는 반드시 배포 전에 프로덕션 모드로 복원할 것**

---

=== 🔧 AI 이미지 생성 OpenAI API 최적화 (2025-09-04) ===

## 주요 수정사항

### 1️⃣ OpenAI 이미지 크기 파라미터 오류 수정
- **문제**: `512x512` 크기가 더 이상 지원되지 않아 400 에러 발생
- **해결**: `1024x1024` 크기로 변경
- **파일**: `apps/app/netlify/functions/generate-meal-image.js`

### 2️⃣ PNG → JPEG 압축 후처리 구현
- **목적**: 용량 최적화 및 업로드 속도 향상
- **구현**: Sharp 라이브러리 사용
- **압축 설정**: 
  - 품질: 85%
  - 점진적 로딩: `progressive: true`
- **성과**: 89% 용량 감소 (1.75MB → 189KB)

### 3️⃣ 실시간 이미지 업데이트 수정
- **문제**: AI 생성 이미지가 프론트엔드에 즉시 표시되지 않음
- **원인**: `school_code` 기반 필터링이 `meal_images` 테이블과 맞지 않음
- **해결**: `meal_id` 기반 필터링으로 변경
- **파일**: `apps/app/src/components/MealImageUploader.tsx`

### 4️⃣ AI 분석 API 파라미터 수정
- **문제**: `schoolName` 대신 `schoolCode` 필요
- **해결**: `AIAnalysisModal`에 `schoolCode` prop 추가
- **파일**: `apps/app/src/components/AIAnalysisModal.tsx`, `apps/app/src/app/battle/page.tsx`

## 성과 요약
- ✅ AI 이미지 생성 API 오류 해결
- ✅ 89% 용량 감소로 업로드 속도 향상
- ✅ 실시간 이미지 표시 정상화
- ✅ AI 분석 API 정상 작동

---

=== 🔧 AI 이미지 생성 시 로그인 풀림 문제 해결 (2025-08-08) ===

## 문제 상황
- **컴퓨터**: AI 사진 등록 → 성공
- **갤럭시폰**: 로그인이 자동으로 풀림
- **재로그인 후**: AI 사진이 정상적으로 등록되어 있다는 알림 표시

## 원인 분석
**Service Role Key 사용으로 인한 세션 충돌**
```javascript
// 문제 코드 (generate-meal-image.js)
const supabase = createClient(
  process.env.NEXT_PUBLIC_SUPABASE_URL,
  process.env.SUPABASE_SERVICE_ROLE_KEY  // ⚠️ 모든 RLS 우회하는 관리자 권한
);
```

**발생 과정**:
1. 컴퓨터에서 AI 이미지 생성 요청
2. Netlify 함수가 Service Role Key로 DB 작업
3. meal_images 테이블에 INSERT → 실시간 알림 전송
4. Service Role 권한으로 인한 세션 충돌 발생
5. 갤럭시폰 로그인 세션 무효화

## 해결 방법

### 1️⃣ 클라이언트: 사용자 토큰 전달
**파일**: `apps/app/src/components/MealImageUploader.tsx`
```typescript
// 사용자 인증 토큰 가져오기
const { data: { session } } = await supabase.auth.getSession();
if (!session?.access_token) {
  throw new Error('로그인이 필요합니다.');
}

// API 요청에 토큰 포함
const response = await fetch(apiUrl, {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': `Bearer ${session.access_token}`, // ✅ 사용자 토큰 전달
  },
  body: JSON.stringify({...})
});
```

### 2️⃣ 서버: Service Role → Anon Key + 사용자 토큰
**파일**: `apps/app/netlify/functions/generate-meal-image.js`
```javascript
// 인증 토큰 확인
const authHeader = event.headers.authorization;
if (!authHeader || !authHeader.startsWith('Bearer ')) {
  return { statusCode: 401, body: JSON.stringify({ success: false, error: '인증 토큰이 필요합니다.' }) };
}

const token = authHeader.replace('Bearer ', '');

// Supabase 클라이언트 초기화 (사용자 토큰 사용)
const supabase = createClient(
  process.env.NEXT_PUBLIC_SUPABASE_URL,
  process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY, // ✅ Service Role → Anon Key 변경
  {
    auth: { persistSession: false },
    global: {
      headers: { Authorization: `Bearer ${token}` } // ✅ 사용자 토큰 사용
    }
  }
);
```

## 해결 효과

### 2️⃣ 포맷 최적화 (PNG → JPEG)
```javascript
// 파일명 생성 (JPEG 포맷)
const fileName = `ai_generated_${meal_id}_${Date.now()}.jpg`;

// Storage 업로드 설정
const { data: fileData, error: uploadError } = await supabaseAdmin.storage
  .from('meal-images')
  .upload(fileName, Buffer.from(base64Image, 'base64'), {
    contentType: 'image/jpeg', // PNG → JPEG 변경
    upsert: true
  });
```

### 3️⃣ 하이브리드 Supabase 클라이언트 구조
```javascript
// 사용자 인증용 클라이언트
const supabase = createClient(
  process.env.NEXT_PUBLIC_SUPABASE_URL,
  process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY,
  {
    auth: { persistSession: false },
    global: {
      headers: { Authorization: `Bearer ${token}` } // 사용자 토큰
    }
  }
);

// Storage 업로드 및 DB 저장용 클라이언트 (RLS 우회)
const supabaseAdmin = createClient(
  process.env.NEXT_PUBLIC_SUPABASE_URL,
  process.env.SUPABASE_SERVICE_ROLE_KEY
);
```

## 성능 개선 효과

### 처리 속도 향상
- ✅ **픽셀 수 감소**: 1536x1024 → 1024x1024 (25% 감소)
- ✅ **타임아웃 방지**: 안정적인 크기로 처리 시간 단축
- ✅ **파일 크기**: JPEG 압축으로 50-70% 감소

### 품질 유지/향상
- ✅ **고품질 설정**: quality="high" (최고 품질)
- ✅ **자연스러운 스타일**: style="natural" 추가
- ✅ **안정적 전송**: Base64 JSON 형식

### 보안 및 안정성
- ✅ **세션 보호**: 사용자 토큰으로 인증
- ✅ **RLS 우회**: Service Role로 Storage/DB 작업
- ✅ **로그인 유지**: 모바일 로그인 풀림 문제 해결

## GPT-Image-1 모델 특성

### 지원 크기 (공식)
- `1024x1024` ✅
- `1536x1024` ✅  
- `1024x1536` ✅
- `auto` (상황에 맞춰 선택)

### 품질 옵션
- `low` / `medium` / `high` / `auto`
- DALL-E 3의 `standard`/`hd`와 표기 다름

### 최적화 권장사항
1. **빠른 처리**: size=1024x1024, quality=medium
2. **고품질**: size=1024x1024, quality=high + 후처리 업스케일
3. **가로형**: size=1536x1024, quality=medium + 즉시 다운스케일

## 커밋 정보
- **커밋**: `aed478b` - "AI이미지새성 프롬프트개선16"
- **변경 파일**: `apps/app/netlify/functions/generate-meal-image.js`
- **주요 변경**: 원본 설정 복원 + 포맷 최적화 + 하이브리드 클라이언트

---

## 🎯 비학생 관심학교 급식 조회 권한 시스템 완성 (2025-08-15)

### ✅ 권한 시스템 개선

#### 1. canViewMeals 권한 추가
**목적**: 비학생이 관심학교 급식을 조회할 수 있도록 읽기 전용 권한 제공

```typescript
// useSchoolMode.tsx 권한 계산
const permissions: Permissions = {
  canComment: hasFullPermissions,
  canRate: hasFullPermissions,
  canLike: hasFullPermissions,
  canUploadPhoto: hasFullPermissions,
  canUseAI: hasFullPermissions,
  canParticipateInBattle: hasFullPermissions,
  canViewMeals: hasReadPermissions, // ← 새로 추가된 읽기 권한
};

// 읽기 권한 조건
const hasReadPermissions = !!(selectedInterestSchool || hasMySchool);
```

#### 2. 권한별 사용자 분류
- **학생 + 내 학교**: 모든 권한 (hasFullPermissions = true)
- **학생 + 관심학교**: 읽기 권한만 (hasReadPermissions = true, hasFullPermissions = false)
- **비학생 + 관심학교**: 읽기 권한만 (hasReadPermissions = true, hasFullPermissions = false)

#### 3. 비학생 자동 모달 시스템
**자동 모달 조건**: 비학생이고 내 학교가 없고 관심학교도 선택되지 않았을 때
```typescript
if (!schoolMode.hasMySchool && !schoolMode.selectedInterestSchool && user.db_profile?.is_student === false) {
  setIsSchoolSearchOpen(true);
}
```

### 🔧 수정된 컴포넌트들

#### 1. MealCard.tsx
- 별점 클릭 시 `canRate` 권한 체크
- 권한 없을 시 "내 학교에서만 가능합니다" 메시지

#### 2. MealImageUploader.tsx  
- 사진 업로드 시 `canUploadPhoto` 권한 체크
- AI 분석 시 `canUseAI` 권한 체크

#### 3. CommentSection.tsx
- 댓글 작성 시 `canComment` 권한 체크
- 좋아요 클릭 시 `canLike` 권한 체크

### 🎯 최종 사용자 경험

#### ✅ 비학생 사용자:
1. 첫 방문 시 자동으로 관심학교 설정 모달 열림
2. 관심학교 설정 후 해당 학교 급식 정보 조회 가능
3. 별점, 댓글, 사진 업로드 등은 제한 (읽기 전용)
4. 페이지 이동 후에도 관심학교 상태 유지

#### ✅ 학생 사용자:
1. 내 학교: 모든 기능 사용 가능
2. 관심학교: 급식 조회만 가능 (상호작용 제한)

**🎉 이제 비학생도 관심학교 급식 정보를 안전하게 조회할 수 있습니다!**